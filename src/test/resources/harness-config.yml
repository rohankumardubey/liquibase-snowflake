inputFormat: xml
context: testContext

databasesUnderTest:
  - name: snowflake
    version:
    url: jdbc:snowflake://{account_id}.snowflakecomputing.com/?db=lbcat&schema=public
    username:
    password:
    dbSchema: PUBLIC
   # paste you snowflake account id and credentials. URL format described here
  # https://docs.snowflake.com/en/user-guide/jdbc-configure.html#connection-parameters
  # SnowflakeChangeObjectIT requires DB pre-populated with data use harness.initScript.sql